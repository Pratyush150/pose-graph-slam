Generated figures and result files land here. Everything in this directory
except this file is ignored by git, because it is all reproducible:

    python3 tools/fetch_datasets.py --all
    python3 benchmarks/run_benchmarks.py

The handful of images the top-level README displays are copied into
`docs/images/` and committed from there.
